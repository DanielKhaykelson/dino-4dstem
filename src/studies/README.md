# studies/

Ablation, sweep, and experiment runner scripts (one-offs used to produce paper results). Not imported by the GUI or assistant. Run directly, e.g. `python src/studies/run_paper_master.py`.
